deploy/
dist/
tmp/
*.tar.gz
*.ipk
*.bak
*.deb
*.log
.DS_Store
cgi-bin/go/

# Внешний форк grdpwasm (grdpwasm/, ~11 МБ с WASM) — артефакт поставки,
# не должен попадать в git. Сборка/деплой из $PROJECT_DIR/grdpwasm.
# (форк — отдельный git-репозиторий со своими правками клиента)
static/
/grdpwasm/

RULES.md

# Dev/workspace artifacts
TECH_SPEC.md
BUILD.md
Install/Install.txt
doc/NETWORK_PROMPT.md
doc/NETDATA_MANUAL.md

# User-specific config files
links.json
auth_config.json
*_config.json
logger/config.json

# Koffe-манифест — личный (уникален у владельца), не в сборке и не в git
bridge/koffe.json
DEVICE.md
DEVLOG.md
go/entware-*
router_backup/
conffiles
control
postinst
prerm
doc/IPK_BUILD.md
forum_post.md
doc/TEST_PLAN.md

# EWM: локальные скилы/агенты и конфиг opencode — только локально
# (содержат пароль роутера, доступы, ip; RULES.md тоже локальный)
.opencode/
opencode.json
